import 'package:flutter/material.dart';

class AppColors {
  // Brand Palette
  static const Color primary = Color(0xFF2563EB);        // Royal Blue
  static const Color primaryHover = Color(0xFF1D4ED8);   // Darker Royal Blue
  static const Color secondary = Color(0xFF7C3AED);      // AI Purple
  static const Color accent = Color(0xFF06B6D4);         // Electric Cyan

  // Canvas & Surfaces
  static const Color bgCanvas = Color(0xFF07090F);       // Deep Dark Canvas
  static const Color bgSurface = Color(0xFF0D1222);      // Card & Surface
  static const Color bgSurfaceHover = Color(0xFF131A30);
  static const Color border = Color(0x14FFFFFF);         // 8% White Border
  static const Color borderActive = Color(0x662563EB);   // Active Blue Border

  // Typography
  static const Color textPrimary = Color(0xFFF8FAFC);    // High contrast white
  static const Color textSecondary = Color(0xFFD1D5DB);  // Subtitles
  static const Color textMuted = Color(0xFF94A3B8);      // Labels
  static const Color textDisabled = Color(0xFF64748B);

  // Statuses
  static const Color success = Color(0xFF10B981);        // Emerald Green
  static const Color successBg = Color(0x2610B981);
  static const Color warning = Color(0xFFF59E0B);        // Amber
  static const Color warningBg = Color(0x26F59E0B);
  static const Color danger = Color(0xFFEF4444);         // Rose Red
  static const Color dangerBg = Color(0x26EF4444);
  static const Color info = Color(0xFF38BDF8);           // Sky Blue
  static const Color infoBg = Color(0x2638BDF8);
}
