{
  "name": "event-os-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "start": "node frontend/gateway.js",
    "build": "npm --prefix frontend run build"
  }
}
